# Loopstack (iOS)

Native SwiftUI + AVAudioEngine looper for iPhone. This is not a WebView wrapper.

The website / web app lives in a separate repo: https://github.com/kevintclayton/Loopstack

Open `Loopstack.xcodeproj` in Xcode, pick a device, and run.
